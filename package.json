{
  "name": "@bicycle-codes/stringify",
  "description": "Create small base64-encoded string representations of an image.",
  "directories": {
    "test": "test"
  },
  "type": "module",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "files": [
    "./dist/*"
  ],
  "scripts": {
    "lint": "eslint \"./**/*.{ts,js}\"",
    "test": "npm run build && esbuild test/index.ts --platform=node --format=esm --keep-names --bundle | node --input-type=module | tap-spec",
    "build-cjs": "esbuild src/*.ts --format=cjs --keep-names --tsconfig=tsconfig.build.json --outdir=./dist --out-extension:.js=.cjs --sourcemap",
    "build-esm": "tsc --project tsconfig.build.json",
    "build": "mkdir -p ./dist && rm -rf ./dist/* && npm run build-cjs && npm run build-esm",
    "preversion": "npm run lint",
    "version": "auto-changelog -p --template keepachangelog --breaking-pattern 'BREAKING CHANGE:' && git add CHANGELOG.md",
    "postversion": "git push --follow-tags && npm publish",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "file-type": "^19.0.0",
    "sharp": "^0.33.4",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@bicycle-codes/debug": "^0.6.8",
    "@bicycle-codes/tapzero": "^0.9.3",
    "@types/node": "^20.12.13",
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "@typescript-eslint/parser": "^7.1.1",
    "auto-changelog": "^2.4.0",
    "esbuild": "^0.21.4",
    "eslint": "^8.57.0",
    "eslint-config-standard": "^17.1.0",
    "tap-spec": "^5.0.0",
    "typescript": "^5.4.5"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./*": {
      "import": [
        "./dist/*.js",
        "./dist/*"
      ],
      "require": [
        "./dist/*.cjs",
        "./dist/*"
      ]
    }
  },
  "author": "nichoth <nichoth@nichoth.com> (https://nichoth.com)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bicycle-codes/stringify.git"
  },
  "homepage": "https://github.com/bicycle-codes/stringify#readme",
  "bugs": {
    "url": "https://github.com/bicycle-codes/stringify/issues"
  },
  "keywords": [
    "stringify",
    "base64"
  ],
  "license": "MIT"
}
